# Python packaging artifacts generated by `pip install -e .` (root .gitignore
# already covers .venv/, __pycache__/, *.pyc, .pytest_cache/).
*.egg-info/

# pytest-cov's SQLite data file from `pytest --cov` runs.
.coverage
