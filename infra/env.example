# Consolidated reference of every env var used across docker-compose.yml's
# services. Copy to infra/.env before running `docker compose up` if you
# want to override any default (compose reads infra/.env automatically).
# Per-app .env.example files (apps/api/.env.example, packages/db/.env.example)
# are the source of truth for local (non-Docker) dev.

# --- api ---
# Leave unset for local dev/CI (docker-compose.yml doesn't set
# NODE_ENV=production, so auth.module.ts's own dev-only fallback applies).
# Only set this if you're reusing this compose file toward a real deployment
# with NODE_ENV=production - in that case it's required (auth.module.ts
# refuses to start without it).
JWT_SECRET=

# Leave all three unset to run with MockBillingProvider (no real Stripe
# account needed for local dev/CI) - required together with JWT_SECRET
# whenever NODE_ENV=production (see billing.service.ts's createBillingProvider).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_PRICE_ID=

# Leave both unset to run with the deterministic MockAiProvider (no AI
# account needed for local dev/CI). If OPENROUTER_API_KEY is set,
# createAiProvider() uses it in preference to ANTHROPIC_API_KEY - a cheap way
# to smoke-test real-model behavior on a free-tier model (openrouter.ai)
# before committing to Anthropic's paid API. OPENROUTER_MODEL overrides the
# default free model slug (OpenRouter's free roster rotates).
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
